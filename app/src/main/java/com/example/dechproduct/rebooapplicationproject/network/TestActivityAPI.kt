package com.example.dechproduct.rebooapplicationproject.network

// Example when we want to use @Get / @Post we will use in this file
// almost will create model first and create these later
/*

import com.example.dechproduct.myloveproject.model.RandomActivity
import com.example.dechproduct.myloveproject.utils.Constants
import io.reactivex.rxjava3.core.Single
import retrofit2.http.GET

interface TestActivityAPI {
     @GET(Constants.API_END_POINT)
    fun getRandomActivity(): Single<TestActivity>
}

*/
